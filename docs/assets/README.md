# Documentation assets

`pipesonata-demo.png` is generated from the production build by `npm run demo`. The script checks
that the PipeSonata workbench loaded before capturing the 1440 by 1000 light-theme viewport.
